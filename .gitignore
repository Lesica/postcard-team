# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Local-only
private/
*.local.md

# TG raw archive (sensitive, локально)
tg_archive/

# Event log + dead letters (содержат raw-тексты сообщений, sensitive)
data/

# Локальные секреты (токен, chat_id, sender_id) — НЕ коммитить
bridge/config_local.py
bridge/config.local.py
